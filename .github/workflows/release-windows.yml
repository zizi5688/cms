name: Release Windows

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag, e.g. v1.0.7'
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-windows-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-windows:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: pip
          cache-dependency-path: scripts/cms-engine-requirements.txt

      - name: Install dependencies
        run: npm ci

      - name: Publish Windows release
        shell: pwsh
        run: |
          if ($env:pythonLocation) {
            $env:CMS_ENGINE_PYTHON = Join-Path $env:pythonLocation "python.exe"
            Write-Host "Using CMS_ENGINE_PYTHON=$env:CMS_ENGINE_PYTHON"
          } else {
            Write-Host "pythonLocation env is empty, fallback to script auto-detection."
          }
          npm run publish:win:x64:ci
