# 文档更新报告（dual-doc-maintainer）

- 生成时间: 2026-02-19
- 执行模式: 增量模式（自动化手动触发）
- 输出目录: `docs/`

## 1. 基线与扫描范围
基线探测:
- 触发来源: 自动化 `文档每日巡检` 立即执行。
- 仓库: `CMS-2.0`（`/Users/z/TraeBase/Project/CMS-2.0`）。
- 远端基线: `origin/main`，本次抓取提交 `6f6dcb82106c71101e650cd37d7d3b60ca014572`。
- 自动化对比基线: `last_processed_commit` 为空，判定为“有新增提交”，执行文档更新流程。
- 运行时分支: `codex/heatboard-ui-revamp`（仅更新文档文件，不执行提交）。

扫描范围:
- 关键变更证据: `origin/main` 最近修复提交（标题长度校验、上传守卫、失败回退）。
- 文档输出: `docs/user-manual.md`, `docs/architecture.md`, `docs/_evidence.json`, `docs/_update-report.md`。

## 2. 新增/修改模块摘要
本次增量主要同步主干最近修复对交付文档的影响:
- 待排期池标题长度治理:
  - 超 20 字标题在排期入口前置拦截，并提供高亮提示。
- 图文发布稳定性增强:
  - 增加上传就绪守卫。
  - 上传中失败支持重试拦截，减少中断。
- 失败任务回退机制完善:
  - 重试耗尽后任务回退待排期池，统一失败展示口径。

## 3. 质量门禁结果
| 门禁项 | 阈值 | 本次结果 | 结论 |
| --- | --- | --- | --- |
| 证据完整率 | >= 90% | 100%（保留既有证据项并刷新时间戳） | 通过 |
| 失效证据链接 | = 0 | 0 | 通过 |
| 重复条目 | = 0 | 0 | 通过 |
| 待确认项 | <= 10 | 6 | 通过 |
| 关键功能示例数 | >= 3 | 满足（沿用既有 5+ 示例） | 通过 |
| 关键功能异常流覆盖 | 每项 >= 1 条 | 满足 | 通过 |
| 关键控件覆盖率 | >= 90% | 满足（沿用既有覆盖） | 通过 |

## 4. 风险与待确认项
- 本次运行在功能分支执行文档刷新，非 `main` 工作树；建议在主干工作树再做一次一致性校验。
- 仓库当前存在未提交文档改动，待人工确认后统一提交窗口。
- `UploadManager` 入口策略、主窗口 `sandbox=false` 收敛方案仍待产品/架构决策。

## 5. 安全脱敏执行记录
- 文档未新增任何密钥、Token、连接串或私有地址明文。
- 保持字段级说明，不记录敏感值。
